== README

Backend REST API for Location based alarms app Hereminder. 
Can send push notifications through APNS and GCM, using pushmeup gem for Rails.
Uses Sidekiq to send notifications in the background.
FB_Graph2 for facebook authentication. 

